vue:
  js:
    https://cdnjs.cloudflare.com/ajax/libs/axios/0.19.0/axios.js: { type: external, preprocess: false }
    https://cdn.jsdelivr.net/npm/vue/dist/vue.js: { type: external, preprocess: false }
